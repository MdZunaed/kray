# Services (Data Layer)

This folder is the data-access layer for Kray (database, external APIs, etc.).

- Use `src/lib/prisma.ts` for DB access.
- Keep business logic in higher layers (features/use-cases), not here.

